<!DOCTYPE html>
<html>

<head>
  <title>Link Shortner</title>
  <style>
    *{ box-sizing:border-box; } 
  body{ background:#ececec; }
  .container{ width:100%; margin-left:1%; margin-top:1%;}
  input[type=url]{ width:80%; margin: 10px 0 10px 0; padding:14px; border:none; font-size:18px; font-family:sans-serif; border-radius:5px; }
  input[type=submit]{  margin: 10px 0 10px 10px; padding:15px; border:none; font-size:18px; font-family:sans-serif; background:orange; color:white; border-radius:5px; cursor:pointer; position:absolute; }
  button{font-family:sans-serif; background:orange; color:white; border-radius:5px; cursor:pointer;}
  .copy{border-radius:5px; width: 50%;}
</style>
</head>

<body>
  <script>
    function copy(){
      document.getElementById("link").select()
      document.execCommand("copy")
      document.getElementById('done').append()
    }
  </script>
  <div class='container'>
    <h1>URL Shortner</h1>
    {{if .error}}
    <span>{{.error}}</span>
    {{end}}
    <form method="POST">
      {{.csrfField}}
      <input class="cell" type="url" name="url" required placeholder="https://google.com">
      <input class="cell button" type="submit" value="Shrink">
    </form>
    {{if .url}}
    <label><strong>Shrinked Link: </strong></label><input class="copy" id="link" type="text" readonly value="{{.url}}"><button
      id="copy" onclick="copy()">Copy</button> <span id="done"></span>
    {{end}}
  </div>
</body>

</html>